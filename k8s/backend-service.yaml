apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: ai-gantt
  labels:
    app: ai-gantt
    version: "0.1.0"
    managed-by: kustomize
spec:
  type: ClusterIP
  selector:
    app: backend
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
